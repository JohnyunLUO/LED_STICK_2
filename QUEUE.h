#ifndef QUEUE_H
#define QUEUE_H

// Circular Queue implementation in C

#include <stdio.h>
#include "USART.h"

#define SIZE 5

#endif

int isFull(void);
int isEmpty(void);
void enQueue(int element);
int deQueue(void);
void display(void);
